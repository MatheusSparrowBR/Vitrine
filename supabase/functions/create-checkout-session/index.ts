import Stripe from 'npm:stripe'
import { createClient } from 'npm:@supabase/supabase-js@2'
const stripe=new Stripe(Deno.env.get('STRIPE_SECRET_KEY')||'')
const supabaseAdmin=createClient(Deno.env.get('SUPABASE_URL')||'',Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||Deno.env.get('SUPABASE_SECRET_KEY')||'')
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS'}
const response=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,'Content-Type':'application/json'}})
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return response({error:'Method not allowed'},405);if(!Deno.env.get('STRIPE_SECRET_KEY'))return response({error:'STRIPE_SECRET_KEY não configurada no Supabase.'},500);const token=(req.headers.get('Authorization')||'').replace(/^Bearer\s+/i,'');if(!token)return response({error:'Não autenticado.'},401);const {data:{user},error:userError}=await supabaseAdmin.auth.getUser(token);if(userError||!user)return response({error:'Sessão inválida.'},401);const body=await req.json().catch(()=>null),businessId=body?.business_id,planCode=body?.plan_code,interval=body?.interval==='yearly'?'yearly':'monthly';if(!businessId||!['pro','premium'].includes(planCode))return response({error:'Empresa, plano e intervalo são obrigatórios.'},400);const {data:business}=await supabaseAdmin.from('businesses').select('id,name,owner_id').eq('id',businessId).eq('owner_id',user.id).maybeSingle();if(!business)return response({error:'Empresa não encontrada ou sem permissão.'},403);const {data:existing}=await supabaseAdmin.from('subscriptions').select('id,provider_subscription_id,status,plan_id').eq('business_id',business.id).in('status',['active','trialing','past_due','unpaid']).eq('provider','stripe').order('created_at',{ascending:false}).limit(1).maybeSingle();if(existing?.provider_subscription_id)return response({error:'Esta empresa já possui uma assinatura Stripe ativa. Use Gerenciar assinatura para alterar ou cancelar.'},409);const {data:plan}=await supabaseAdmin.from('plans').select('id,code,name,price_monthly,price_yearly,stripe_price_monthly_id,stripe_price_yearly_id').eq('code',planCode).eq('active',true).maybeSingle();if(!plan)return response({error:'Plano não encontrado.'},404);const priceId=interval==='yearly'?plan.stripe_price_yearly_id:plan.stripe_price_monthly_id;const origin=Deno.env.get('SITE_URL')||req.headers.get('origin')||'';if(!origin)return response({error:'SITE_URL não configurada.'},500);const metadata={business_id:business.id,user_id:user.id,plan_id:plan.id,plan_code:plan.code,billing_interval:interval};const lineItem=priceId?{price:priceId,quantity:1}:{price_data:{currency:'brl',unit_amount:Math.round(Number(interval==='yearly'?plan.price_yearly:plan.price_monthly)*100),product_data:{name:`VitrineLocal ${plan.name}`},recurring:{interval:interval==='yearly'?'year':'month'}},quantity:1};const session=await stripe.checkout.sessions.create({mode:'subscription',line_items:[lineItem],customer_email:user.email||undefined,allow_promotion_codes:true,success_url:`${origin}/planos?checkout=success`,cancel_url:`${origin}/planos?checkout=cancelled`,client_reference_id:business.id,metadata,subscription_data:{metadata}});return response({url:session.url,id:session.id})})
