import React,{useEffect,useMemo,useState} from 'react'
import {createClient} from '@supabase/supabase-js'
import PlanUsagePanel from './PlanUsageReact.jsx'
import BusinessHoursEditor from './BusinessHoursEditor.jsx'
import './core.css'
import './business-hours.css'
const URL=import.meta.env.VITE_SUPABASE_URL
const KEY=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY
const db=URL&&KEY?createClient(URL,KEY):null
const MEDIA_BUCKET='business-media'
const IMAGE_TYPES=new Set(['image/jpeg','image/png','image/webp','image/gif'])
const VIDEO_TYPES=new Set(['video/mp4','video/webm','video/quicktime'])
const EMPTY_STATS={profile_view:0,whatsapp_click:0,instagram_click:0,website_click:0}
const EMPTY_FORM={name:'',short_description:'',description:'',phone:'',whatsapp:'',website_url:'',instagram_url:'',facebook_url:'',address:'',neighborhood:'',category_id:'',opening_hours:{}}
export default function AccountPage(){const[session,setSession]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{let live=true;async function loadSession(){if(!db){setLoading(false);return}const{data}=await db.auth.getSession();if(live){setSession(data.session||null);setLoading(false)}}loadSession();const subscription=db?.auth.onAuthStateChange((_event,nextSession)=>setSession(nextSession||null));return()=>{live=false;subscription?.data?.subscription?.unsubscribe()}},[]);if(loading)return <div className="app"><main className="page section"><div className="empty"><h3>Carregando sua conta…</h3></div></main></div>;if(!session)return <div className="app"><main className="page section"><div className="empty"><h1>Entre para acessar sua conta</h1><p>O painel da empresa fica disponível para usuários autenticados.</p><a className="btn primary" href="/login?next=%2Fconta">Entrar</a></div></main></div>;return <OwnerDashboard session={session}/>}
function OwnerDashboard({session}){const[businesses,setBusinesses]=useState([]),[selected,setSelected]=useState(null),[categories,setCategories]=useState([]),[photos,setPhotos]=useState([]),[items,setItems]=useState([]),[promotions,setPromotions]=useState([]),[stats,setStats]=useState(EMPTY_STATS),[form,setForm]=useState(EMPTY_FORM),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[message,setMessage]=useState({text:'',error:false});const notify=(text,error=false)=>{setMessage({text:String(text||''),error});window.clearTimeout(window.__vlAccountToast);window.__vlAccountToast=window.setTimeout(()=>setMessage({text:'',error:false}),3500)};async function loadBusinesses(){if(!db){setLoading(false);return}setLoading(true);const[businessRes,categoriesRes]=await Promise.all([db.from('businesses').select('id,name,status,city_id,category_id').eq('owner_id',session.user.id).order('created_at',{ascending:false}),db.from('categories').select('id,name,slug,icon').eq('active',true).order('sort_order').order('name')]);if(businessRes.error)notify(businessRes.error.message,true);if(categoriesRes.error)notify(categoriesRes.error.message,true);const rows=businessRes.data||[];setBusinesses(rows);setCategories(categoriesRes.data||[]);if(!selected&&rows[0])setSelected(rows[0]);if(selected){const fresh=rows.find(row=>row.id===selected.id);if(fresh)setSelected(fresh);else setSelected(rows[0]||null)}setLoading(false)}async function loadBusiness(id){if(!db||!id)return;const[businessRes,photosRes,itemsRes,promotionsRes,analyticsRes]=await Promise.all([db.from('businesses').select('*,categories(id,name,slug,icon),cities(id,name,state,slug)').eq('id',id).single(),db.from('business_photos').select('*').eq('business_id',id).order('sort_order'),db.from('business_items').select('*').eq('business_id',id).order('sort_order'),db.from('promotions').select('id,title,status,price,starts_at,ends_at').eq('business_id',id).order('created_at',{ascending:false}),db.from('analytics_events').select('event_type').eq('business_id',id)]);if(businessRes.error){notify(businessRes.error.message,true);return}const business=businessRes.data;setSelected(business);setPhotos(photosRes.data||[]);setItems(itemsRes.data||[]);setPromotions(promotionsRes.data||[]);const nextStats={...EMPTY_STATS};(analyticsRes.data||[]).forEach(row=>{if(Object.prototype.hasOwnProperty.call(nextStats,row.event_type))nextStats[row.event_type]+=1});setStats(nextStats);setForm({name:business.name||'',short_description:business.short_description||'',description:business.description||'',phone:business.phone||'',whatsapp:business.whatsapp||'',website_url:business.website_url||'',instagram_url:business.instagram_url||'',facebook_url:business.facebook_url||'',address:business.address||'',neighborhood:business.neighborhood||'',category_id:business.category_id||'',opening_hours:business.opening_hours||{}})}useEffect(()=>{loadBusinesses()},[]);useEffect(()=>{if(selected?.id)loadBusiness(selected.id)},[selected?.id]);const cityLabel=useMemo(()=>selected?.cities?`${selected.cities.name} - ${selected.cities.state}`:'Cidade não informada',[selected?.cities]);function updateField(key,value){setForm(current=>({...current,[key]:value}))}async function save(){if(!db||!selected)return;setSaving(true);const{error}=await db.from('businesses').update(form).eq('id',selected.id);if(error)notify(error.message,true);else{notify('Dados da empresa atualizados.');await loadBusiness(selected.id);await loadBusinesses()}setSaving(false)}async function uploadMedia(file,kind){if(!db||!selected||!file)return;const valid=kind==='photo'?IMAGE_TYPES.has(file.type)||VIDEO_TYPES.has(file.type):IMAGE_TYPES.has(file.type);if(!valid)return notify('Formato de arquivo não suportado.',true);if(file.size>50*1024*1024)return notify('Arquivo acima de 50 MB.',true);const extensionByType={'image/jpeg':'jpg','image/png':'png','image/webp':'webp','image/gif':'gif','video/mp4':'mp4','video/webm':'webm','video/quicktime':'mov'};const path=`${selected.id}/${crypto.randomUUID()}.${extensionByType[file.type]||'bin'}`;const upload=await db.storage.from(MEDIA_BUCKET).upload(path,file,{cacheControl:'31536000',contentType:file.type,upsert:false});if(upload.error)return notify(upload.error.message,true);const publicUrl=db.storage.from(MEDIA_BUCKET).getPublicUrl(path).data.publicUrl;if(kind==='logo'||kind==='cover'){const field=`${kind}_url`,pathField=`${kind}_path`;const{error}=await db.from('businesses').update({[field]:publicUrl,[pathField]:path}).eq('id',selected.id);if(error){await db.storage.from(MEDIA_BUCKET).remove([path]).catch(()=>{});return notify(error.message,true)}notify('Imagem atualizada.')}else{const{error}=await db.from('business_photos').insert({business_id:selected.id,image_url:publicUrl,image_path:path,sort_order:photos.length});if(error){await db.storage.from(MEDIA_BUCKET).remove([path]).catch(()=>{});return notify(error.message,true)}notify('Mídia adicionada.')}await loadBusiness(selected.id)}async function deletePhoto(photo){if(!db||!selected)return;const{error}=await db.from('business_photos').delete().eq('id',photo.id);if(error)return notify(error.message,true);if(photo.image_path)await db.storage.from(MEDIA_BUCKET).remove([photo.image_path]).catch(()=>{});notify('Mídia removida.');await loadBusiness(selected.id)}async function addItem(){if(!db||!selected)return;const name=window.prompt('Nome do produto/serviço');if(!name?.trim())return;const description=window.prompt('Descrição (opcional)')||null;const priceInput=window.prompt('Preço (opcional)');const price=priceInput?Number(priceInput.replace(',','.')):null;const{error}=await db.from('business_items').insert({business_id:selected.id,name:name.trim(),description,price:Number.isFinite(price)?price:null,active:true,sort_order:items.length});if(error)return notify(error.message,true);notify('Produto/serviço adicionado.');await loadBusiness(selected.id)}async function deleteItem(item){if(!db||!selected)return;const{error}=await db.from('business_items').delete().eq('id',item.id);if(error)return notify(error.message,true);notify('Produto/serviço removido.');await loadBusiness(selected.id)}async function addPromotion(){if(!db||!selected)return;const title=window.prompt('Título da promoção');if(!title?.trim())return;const description=window.prompt('Descrição')||null;const priceInput=window.prompt('Preço promocional (opcional)');const price=priceInput?Number(priceInput.replace(',','.')):null;const{error}=await db.from('promotions').insert({business_id:selected.id,title:title.trim(),description,price:Number.isFinite(price)?price:null,status:'pending_review'});if(error)return notify(error.message,true);notify('Promoção enviada para revisão.');await loadBusiness(selected.id)}if(loading&&businesses.length===0)return <div className="app"><main className="page section"><div className="empty"><h3>Carregando conta…</h3></div></main></div>;return <div className="app"><header className="topbar"><div className="nav"><a className="brand" href="/laguna"><span className="brand-mark">V</span><span>Vitrine<span className="brand-accent">Local</span></span></a><div className="nav-spacer"/><nav className="nav-actions"><a href="/planos">Planos</a><a href="/laguna">Voltar</a><button onClick={async()=>{await db?.auth.signOut();location.href='/laguna'}}>Sair</button></nav></div></header><main className="page"><div className="dashboard-header"><div><span className="section-kicker">MINHA CONTA</span><h1>Painel da empresa</h1><p>Gerencie dados, mídias, produtos, promoções e acompanhe o consumo do plano.</p></div><span className="muted">{cityLabel}</span></div>{message.text&&<div className={`empty ${message.error?'danger-text':''}`} style={{marginBottom:16}}>{message.text}</div>}{businesses.length===0?<div className="empty"><h3>Você ainda não possui uma empresa</h3><p>Volte à página inicial e use o botão de cadastro para enviar sua empresa.</p><a className="btn primary" href="/laguna">Voltar ao site</a></div>:<><div className="stats">{[['profile_view','Visualizações'],['whatsapp_click','WhatsApp'],['instagram_click','Instagram'],['website_click','Site']].map(([key,label])=><div className="stat" key={key}><span>{label}</span><strong>{stats[key]}</strong><small>eventos registrados</small></div>)}</div><div className="owner-layout"><aside className="panel"><h3>Minhas empresas</h3><div className="business-picker">{businesses.map(business=><button key={business.id} className={selected?.id===business.id?'active':''} onClick={()=>setSelected(business)}><strong>{business.name}</strong><small className="muted">{business.status}</small></button>)}</div></aside>{selected&&<section><PlanUsagePanel businessId={selected.id}/><div className="panel"><h3>Dados da empresa</h3><div className="form-grid"><label className="field"><span>Nome</span><input value={form.name} onChange={e=>updateField('name',e.target.value)}/></label><label className="field"><span>Categoria</span><select value={form.category_id} onChange={e=>updateField('category_id',e.target.value)}><option value="">Selecione</option>{categories.map(category=><option value={category.id} key={category.id}>{category.name}</option>)}</select></label><label className="field full"><span>Descrição curta</span><input value={form.short_description} onChange={e=>updateField('short_description',e.target.value)} maxLength={180}/></label><label className="field full"><span>Descrição</span><textarea value={form.description} onChange={e=>updateField('description',e.target.value)} rows="5"/></label><label className="field"><span>Telefone</span><input value={form.phone} onChange={e=>updateField('phone',e.target.value)}/></label><label className="field"><span>WhatsApp</span><input value={form.whatsapp} onChange={e=>updateField('whatsapp',e.target.value)}/></label><label className="field"><span>Site</span><input value={form.website_url} onChange={e=>updateField('website_url',e.target.value)}/></label><label className="field"><span>Instagram</span><input value={form.instagram_url} onChange={e=>updateField('instagram_url',e.target.value)}/></label><label className="field"><span>Facebook</span><input value={form.facebook_url} onChange={e=>updateField('facebook_url',e.target.value)}/></label><label className="field"><span>Bairro</span><input value={form.neighborhood} onChange={e=>updateField('neighborhood',e.target.value)}/></label><label className="field full"><span>Endereço</span><input value={form.address} onChange={e=>updateField('address',e.target.value)}/></label></div><div className="panel" style={{marginTop:16,padding:18}}><h3 style={{marginTop:0}}>Horário de funcionamento</h3><BusinessHoursEditor value={form.opening_hours} onChange={value=>updateField('opening_hours',value)}/></div><div className="form-actions"><button className="btn primary" disabled={saving} onClick={save}>{saving?'Salvando…':'Salvar alterações'}</button></div></div><div className="panel" style={{marginTop:16}}><h3>Imagem da empresa</h3><div className="upload-box"><label>Logo<input type="file" accept="image/jpeg,image/png,image/webp,image/gif" onChange={e=>uploadMedia(e.target.files?.[0],'logo')}/></label><label>Capa<input type="file" accept="image/jpeg,image/png,image/webp,image/gif" onChange={e=>uploadMedia(e.target.files?.[0],'cover')}/></label></div></div><div className="panel" style={{marginTop:16}}><div className="mini-row"><strong>Mídias</strong><button className="small-btn" onClick={()=>document.getElementById('account-media-input')?.click()}>Adicionar</button></div><input id="account-media-input" hidden type="file" accept="image/jpeg,image/png,image/webp,image/gif,video/mp4,video/webm,video/quicktime" onChange={e=>uploadMedia(e.target.files?.[0],'photo')}/>{photos.length?<div className="gallery-owner">{photos.map(photo=><figure key={photo.id}>{photo.video_url?<video src={photo.video_url} controls/>:<img src={photo.image_url} alt=""/>}<figcaption><button className="small-btn danger-text" onClick={()=>deletePhoto(photo)}>Excluir</button></figcaption></figure>)}</div>:<div className="empty"><p>Nenhuma mídia adicionada.</p></div>}</div><div className="panel" style={{marginTop:16}}><div className="mini-row"><strong>Produtos e serviços</strong><button className="small-btn" onClick={addItem}>Adicionar</button></div>{items.length?items.map(item=><div className="mini-row" key={item.id}><span><strong>{item.name}</strong><small>{item.description||'Sem descrição'}</small></span><button className="small-btn danger-text" onClick={()=>deleteItem(item)}>Excluir</button></div>):<div className="empty"><p>Nenhum produto ou serviço.</p></div>}</div><div className="panel" style={{marginTop:16}}><div className="mini-row"><strong>Promoções</strong><button className="small-btn" onClick={addPromotion}>Criar promoção</button></div>{promotions.length?promotions.map(promotion=><div className="mini-row" key={promotion.id}><span><strong>{promotion.title}</strong><small>{promotion.status}</small></span></div>):<div className="empty"><p>Nenhuma promoção cadastrada.</p></div>}</div></section>}</div></>}</main></div>}
