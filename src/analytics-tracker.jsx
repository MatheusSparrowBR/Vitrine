import React,{useEffect} from 'react'
import {createClient} from '@supabase/supabase-js'
const U=import.meta.env.VITE_SUPABASE_URL,K=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY,db=U&&K?createClient(U,K):null
function sessionId(){try{let id=localStorage.getItem('vl_analytics_session');if(!id){id=crypto.randomUUID();localStorage.setItem('vl_analytics_session',id)}return id}catch{return null}}
async function record(type,extra={}){if(!db)return;const p=location.pathname.split('/').filter(Boolean);const citySlug=p[0]&&!['login','planos','conta','admin','privacidade','termos','atualizar-senha'].includes(p[0])?p[0]:null;let city_id=extra.city_id||null,business_id=extra.business_id||null;try{if(citySlug&&!city_id){const{data:c}=await db.from('cities').select('id').eq('slug',citySlug).maybeSingle();city_id=c?.id||null}if(citySlug&&p[1]==='empresa'&&p[2]&&!business_id){const{data:b}=await db.from('businesses').select('id,city_id').eq('slug',decodeURIComponent(p[2])).maybeSingle();business_id=b?.id||null;city_id=city_id||b?.city_id||null}await db.from('analytics_events').insert({event_type:type,session_id:sessionId(),user_id:null,city_id,business_id,metadata:extra})}catch{}}
export default function AnalyticsTracker(){useEffect(()=>{if(location.pathname.startsWith('/admin')||location.pathname==='/login'||location.pathname==='/atualizar-senha')return;record('page_view');const click=e=>{const a=e.target.closest?.('a,button');if(!a)return;const href=a.href||'';const text=(a.innerText||'').trim();if(href.includes('wa.me')||/whatsapp/i.test(text))record('whatsapp_click');else if(/instagram\.com/i.test(href)||/instagram/i.test(text))record('instagram_click');else if(a.matches?.('[href*="/empresa/"]'))record('business_click');else if(a.matches?.('[href*="/promocoes"]'))record('promotion_click');else if(a.matches?.('[href*="/eventos"]'))record('event_click');else if(a.matches?.('[href*="categoria="]'))record('category_click',{category:new URL(href,location.origin).searchParams.get('categoria')});else if(a.classList.contains('vl-premium-real'))record('banner_click')};document.addEventListener('click',click);return()=>document.removeEventListener('click',click)},[location.pathname]);return null}
