import React,{useEffect,useState} from 'react'
import {createClient} from '@supabase/supabase-js'
import './city-home.css'
const URL=import.meta.env.VITE_SUPABASE_URL
const KEY=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY
const db=URL&&KEY?createClient(URL,KEY):null
const FALLBACK={id:'fallback',name:'Laguna',state:'SC',slug:'laguna',active:true}
const FALLBACK_CATS=[['Restaurantes','🍽️'],['Lojas','🛍️'],['Serviços','🧰'],['Saúde','❤️'],['Beleza','✨'],['Turismo','📍'],['Automóveis','🚗'],['Imóveis','🏠'],['Pets','🐾'],['Outros','✦']]
const slugify=s=>String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')
export default function CityHomePage({citySlug='laguna'}){
 const [cities,setCities]=useState([]),[city,setCity]=useState(null),[session,setSession]=useState(null),[role,setRole]=useState('user'),[categories,setCategories]=useState([]),[businesses,setBusinesses]=useState([]),[promotions,setPromotions]=useState([]),[events,setEvents]=useState([]),[banner,setBanner]=useState(null),[q,setQ]=useState(''),[loading,setLoading]=useState(true)
 useEffect(()=>{if(!db){setCity(citySlug==='laguna'?FALLBACK:{...FALLBACK,slug:citySlug,name:citySlug});setCategories(FALLBACK_CATS.map(([name,icon],i)=>({id:i,name,icon,slug:slugify(name)})));setLoading(false);return}let alive=true;Promise.all([db.auth.getSession(),db.from('cities').select('id,name,state,slug,active').eq('active',true).order('name')]).then(([a,c])=>{if(!alive)return;setSession(a.data.session||null);const rows=c.data||[];setCities(rows);setCity(rows.find(x=>x.slug===citySlug)||rows[0]||null)});const sub=db.auth.onAuthStateChange((_e,s)=>setSession(s||null));return()=>{alive=false;sub.data.subscription.unsubscribe()}},[citySlug])
 useEffect(()=>{if(!db||!session?.user?.id)return;db.from('profiles').select('role').eq('id',session.user.id).maybeSingle().then(({data})=>setRole(data?.role||'user'))},[session?.user?.id])
 useEffect(()=>{if(!db||!city?.id)return;let alive=true;setLoading(true);Promise.all([db.from('categories').select('id,name,slug,icon').eq('active',true).order('sort_order').order('name'),db.from('businesses').select('id,name,slug,short_description,cover_url,address,featured,categories(name)').eq('city_id',city.id).eq('status','active').order('featured',{ascending:false}).order('created_at',{ascending:false}).limit(8),db.from('promotions').select('id,title,description,businesses!inner(name,slug,city_id)').eq('status','published').eq('businesses.city_id',city.id).order('created_at',{ascending:false}).limit(4),db.from('events').select('id,title,description,image_url,event_date,start_time,location,featured').eq('city_id',city.id).eq('active',true).gte('event_date',new Date().toISOString().slice(0,10)).order('featured',{ascending:false}).order('event_date').order('start_time').limit(4),db.from('advertisements').select('id,title,description,image_url,target_url,priority,starts_at,ends_at').eq('city_id',city.id).eq('placement','home_banner').eq('active',true).order('priority',{ascending:false}).order('created_at',{ascending:false}).limit(10)]).then(([c,b,p,e,a])=>{if(!alive)return;setCategories(c.data?.length?c.data:FALLBACK_CATS.map(([name,icon],i)=>({id:i,name,icon,slug:slugify(name)})));setBusinesses(b.data||[]);setPromotions(p.data||[]);setEvents(e.data||[]);const now=Date.now();setBanner((a.data||[]).find(x=>(!x.starts_at||new Date(x.starts_at)<=now)&&(!x.ends_at||new Date(x.ends_at)>=now))||null);setLoading(false)}).catch(()=>setLoading(false));return()=>{alive=false}},[city?.id])
 const filtered=businesses.filter(b=>{const x=q.trim().toLowerCase();return !x||[b.name,b.short_description,b.description,b.address,b.categories?.name].filter(Boolean).join(' ').toLowerCase().includes(x)})
 const go=path=>location.href=path
 return <div className="vl-public-app"><header className="topbar"><div className="nav"><a className="brand" href={`/${city?.slug||citySlug}`}><span className="brand-mark">V</span><span>Vitrine<span className="brand-accent">Local</span></span></a>{cities.length?<select className="city-select" value={city?.slug||''} onChange={e=>go(`/${e.target.value}`)} aria-label="Selecionar cidade">{cities.map(c=><option key={c.id} value={c.slug}>📍 {c.name} - {c.state}</option>)}</select>:<span className="city-select static">📍 {city?.name||'Laguna'} - {city?.state||'SC'}</span>}<div className="nav-spacer"/><nav className="nav-actions"><a href={`/${city?.slug||citySlug}/empresas`}>Explorar</a><a href={`/${city?.slug||citySlug}/promocoes`}>Promoções</a><a href={`/${city?.slug||citySlug}/eventos`}>Eventos</a><a href="/planos">Planos</a>{role==='admin'&&<a href="/admin">Admin</a>}{session?<><a href="/conta">Minha conta</a><button className="logout" onClick={async()=>{await db?.auth.signOut();location.reload()}}>SAIR DA CONTA</button></>:<a className="primary" href="/login">Entrar</a>}<a className="outline" href={session?'/conta?new=business':'/login?next=%2Fconta%3Fnew%3Dbusiness'}>Cadastrar empresa</a></nav></div></header><section className="hero"><div className="hero-inner"><div><div className="eyebrow">A cidade na palma da mão</div><h1>Descubra o que <span>{city?.name||'sua cidade'}</span> tem de melhor.</h1><p>Encontre empresas, serviços, promoções e eventos perto de você.</p><div className="searchbox"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="O que você procura hoje?"/><button onClick={()=>go(`/${city?.slug||citySlug}/empresas`)}>Buscar</button></div><div className="hero-trust">● Catálogo local de {city?.name||'sua cidade'}.</div></div><div className="hero-card"><div className="mini-header"><span>🔥 Em alta hoje</span><span>{city?.name||'—'}</span></div><div className="pulse-row"><div className="pulse-icon">📣</div><div><strong>Promoções locais</strong><p>Ofertas publicadas por empresas locais</p></div></div><div className="pulse-row"><div className="pulse-icon">📅</div><div><strong>{events.length} eventos próximos</strong><p>Veja o que acontece na cidade</p></div></div><button className="hero-cta" onClick={()=>go(`/${city?.slug||citySlug}/eventos`)}>Ver agenda →</button></div></div></section><section className="page section vl-category-slot"><div className="category-grid">{categories.map(c=><a className="category-card" key={c.id} href={`/${city?.slug||citySlug}/empresas`}><span className="category-icon">{c.icon||'✦'}</span><strong>{c.name}</strong><span>Explorar</span></a>)}</div></section><section className="page section vl-premium-slot"><div className="vl-premium-card">{banner?<a className="vl-premium-real" href={banner.target_url||'#'} target={banner.target_url?'_blank':undefined} rel="noreferrer"><div className="vl-premium-media">{banner.image_url?<img src={banner.image_url} alt={banner.title||'Banner patrocinado'} />:<div className="cover-placeholder">V</div>}</div><div className="vl-premium-copy"><span className="section-kicker">ESPAÇO PUBLICITÁRIO · PREMIUM</span><h2>{banner.title}</h2><p>{banner.description||'Oferta em destaque na VitrineLocal.'}</p></div></a>:<div className="vl-premium-empty"><div className="vl-premium-empty-icon">★</div><div><span className="section-kicker">ESPAÇO PUBLICITÁRIO</span><p>Este espaço é reservado para empresas que desejam colocar sua promoção em destaque na página inicial.</p><strong>Entre em contato para anunciar no VitrineLocal.</strong></div></div>}</div></section>{filtered.length>0&&<section className="page section"><div className="section-head"><div><span className="section-kicker">NEGÓCIOS LOCAIS</span><h2>Empresas em destaque</h2></div><a className="link" href={`/${city?.slug||citySlug}/empresas`}>Ver todas →</a></div><div className="business-grid">{filtered.slice(0,4).map(b=><a className="business-card" key={b.id} href={`/${city?.slug||citySlug}/empresa/${encodeURIComponent(b.slug)}`}><div className="business-cover">{b.cover_url?<img src={b.cover_url} alt="" loading="lazy"/>:<div className="cover-placeholder">V</div>}</div><div className="business-body"><span className="section-kicker">{b.categories?.name||'Empresa'}</span><h3>{b.name}</h3><p>{b.short_description||'Conheça este negócio local.'}</p>{b.address&&<small>{b.address}</small>}</div></a>)}</div></section>}{promotions.length>0&&<section className="section soft"><div className="page"><div className="section-head"><div><span className="section-kicker">OFERTAS</span><h2>Promoções locais</h2></div><a className="link" href={`/${city?.slug||citySlug}/promocoes`}>Ver todas →</a></div><div className="promo-grid">{promotions.slice(0,3).map(p=><a className="content-card" key={p.id} href={`/${city?.slug||citySlug}/promocoes`}><span className="section-kicker">PROMOÇÃO</span><h3>{p.title}</h3><p>{p.description||'Confira esta oferta.'}</p><small>{p.businesses?.name}</small></a>)}</div></div></section>}<section className="page section"><div className="section-head"><div><span className="section-kicker">AGENDA</span><h2>Próximos eventos</h2></div><a className="link" href={`/${city?.slug||citySlug}/eventos`}>Ver agenda →</a></div>{loading?<div className="empty"><h3>Carregando…</h3></div>:events.length?<div className="events-preview-grid">{events.slice(0,3).map(e=><a className="content-card vl-event-preview" key={e.id} href={`/${city?.slug||citySlug}/eventos`}>{e.image_url&&<img src={e.image_url} alt="" loading="lazy"/>}<span className="section-kicker">{new Date(`${e.event_date}T00:00:00`).toLocaleDateString('pt-BR',{day:'2-digit',month:'short'})}</span><h3>{e.title}</h3><p>{e.location||'Local a confirmar'}{e.start_time?` · ${String(e.start_time).slice(0,5)}`:''}</p></a>)}</div>:<div className="empty"><h3>Nenhum evento próximo.</h3><p>Quando o administrador cadastrar novos eventos, eles aparecerão aqui.</p></div>}</section></div>}
